machine:
  ruby:
    version: rbx-2.2.6

general:
  build_dir: \src\test\java\uk\co\jemos\podam\test
  artifacts:
     - $CIRCLE_TEST_REPORTS
     - target  

test:
 post:
   - mkdir -p $CIRCLE_TEST_REPORTS/junit/;
   - find . -type f -regex ".*/target/failsafe-reports/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \; 
